# Test configuration yaml file

# Default chunksize to be used throughout
chunksize: 10

# BigQuery Parameters
gbq:
  credentials: "../tests_output/genie_credentials.json"
  proj: "genie-275215"
  dataset: "test"